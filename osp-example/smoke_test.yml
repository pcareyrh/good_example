---
- hosts: jumpbox

  tasks:
  - name: test app is up
    uri:
      url: "{% for host in groups['frontends'] %}
        {{ hostvars[host]['hostname'] }}
        {% endfor %}"
      status_code: 200
      return_content: yes