---
- hosts: all
  gather_facts: false
  tasks:
    - name: dummy task
      setup:

  roles:
    - { role: provision_aws }
      #    - { role: create_auth }

              #- import_playbook: aws/main.yml
              #
              #

